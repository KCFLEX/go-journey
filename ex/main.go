package main

import "fmt"
var slide int8 = 127
var glide uint8 = 255

func main () {
	//b := 747
	//c := 911
	//d := 90210
	//fmt.Printf("decimal is %d, binary is %b\t  while hexadecimal is %#X\n", b, b, b)
	//fmt.Printf("decimal is %d, binary is %b\t  while hexadecimal is %#x\n", c, c, c)
	//fmt.Printf("decimal is %d, binary is %b\t  while hexadecimal is %#x\n", d, d, d)
    fmt.Printf("the maximum no in int8 =\n %v with type %T\n", slide, slide)
    fmt.Printf("the maximum no in uint8 =\n %v with type %T", glide, glide)

	
}


